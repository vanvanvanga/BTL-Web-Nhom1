<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= item.title %></title>
</head>
<body>
  <h1><%= item.title %></h1>
  <img src="/img/covers/<%= item.title %>.jpg" alt="<%= item.title %>.jpg"/>
  <p>Tác giả: <%= item.author %></p>
  <p>Nội dung: <%= item.synopsis %></p>
  <h2>Mượn sách</h2>
  <p>Hiện thư viện có <%= status.length %> quyển sách này.</p>
  <table>
    <tr>
      <th>Mã quyển sách</th>
      <th>Trạng thái</th>
    </tr>
  <% status.forEach(item=> { %>
    <tr>
      <td><%= item.copy_id %></td>
      <td><% if (item.username) { %>
        Đang được mượn.
      <% } else { %>
        Sẵn sàng cho mượn.
      <% } %></td>
    </tr>
    <% }) %>
  </table>
</body>
</html>